Donwload and install nodejs and npm from:
https://nodejs.org/en/download/

open your commandline tool, for example cmd, navigate to this directory
run npm install to install the packages

modify parameters in reload.js
sample inputs:
const interval = 5000 //5 seconds
const serverurl = "localhost" // localhost
const appname = "the_movies" //app name

run this in command line:
node reload